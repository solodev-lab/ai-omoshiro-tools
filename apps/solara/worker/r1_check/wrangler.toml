# R1 検証用 minimal Worker (本番には deploy しない、検証後に削除可)
# 起動: cd apps/solara/worker && npx wrangler dev --config r1_check/wrangler.toml --port 8788
name = "solara-r1-check"
main = "src/index.js"
compatibility_date = "2024-12-01"
compatibility_flags = ["nodejs_compat"]
workers_dev = false
