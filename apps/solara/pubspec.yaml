name: solara
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+4

environment:
  sdk: ^3.11.3

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^8.0.2
  # 200 字 cap などのため grapheme cluster 数を取りたい用途で characters API を明示利用
  # (Flutter SDK の transitive dep だが direct import するため pubspec に明示)
  characters: ^1.4.1
  shared_preferences: ^2.5.5
  flutter_riverpod: ^3.3.1
  riverpod_annotation: ^4.0.2
  flutter_map: ^8.3.0
  latlong2: ^0.9.1
  http: ^1.6.0
  share_plus: ^11.1.0
  path_provider: ^2.1.5
  geolocator: ^14.0.0
  # Phase 2-6b: RevenueCat 配線 (サブスク + Trusted Entitlements informational)
  purchases_flutter: ^10.1.0
  # Phase 2-6b: ペイウォール内の利用規約 / プライバシー / 解約案内リンクを開く
  url_launcher: ^6.3.2
  # Phase 2-9: Sign in 統合 (Apple Guideline 5.4 — Google 提供時は Apple 必須)
  # Solara は anonymous 購入も許容するが、Sign in で端末跨ぎ復元が安定する
  sign_in_with_apple: ^8.0.0
  google_sign_in: ^7.2.0
  # Phase 2 RASP: root/jailbreak/debugger/emulator/hook 検知 (talsec)
  # security_principles 原則「クライアント改変対策」。検知時は Pro 機能のみ
  # disable、Free 機能は使えるまま (ストア審査対応)。
  freerasp: ^7.5.1
  # Phase 1 App Attest (Solara worker /protected/* middleware と対応)
  # iOS 14+ 実機のみ、bam.tech 製、pub points 150/150
  # Worker 側: apps/solara/worker/src/auth/app_attest.js (verifyAttestation/verifyAssertion)
  # 設計: apps/solara/docs/app_attest_design.md (v2.0+)
  app_attest_integrity: ^1.0.0
  # crypto: SHA-256 で payload hash 計算 (X-AppAttest-Assertion 生成時)
  crypto: ^3.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  riverpod_generator: ^4.0.3
  build_runner: ^2.4.14
  flutter_launcher_icons: ^0.14.3

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/solara_logo.png"
  adaptive_icon_background: "#080C14"
  adaptive_icon_foreground: "assets/solara_logo.png"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/tarot_planet_map.json
    - assets/celestial_events_2026.json
    - assets/solara_logo.png
    - assets/constellation-art/
    - assets/card-images/
    - assets/class-cards/
    - assets/diagnosis-bg/
    - assets/zodiac-symbols/
    - assets/menu_icons/
    - assets/catasterism_bg.webp
    - assets/catasterism_bg_void2.webp
    - assets/catasterism-bg/bright/
    # 2026-05-19: ★4 / ★3 / ★1-2 を「形容詞色 × 2 ベース星座 (ランダム選択)」に
    # 仕様変更。旧 mystical/ + lite/ は廃止して新 variants_* に置換。
    - assets/catasterism-bg/variants_leo/
    - assets/catasterism-bg/variants_virgo/
    - assets/catasterism-bg/variants_scorpio/
    - assets/catasterism-bg/variants_aquarius/
    - assets/catasterism-bg/variants_pisces/
    - assets/catasterism-bg/variants_aries/
    - assets/horo-bg/
    - assets/tarot_scene/
    - assets/tarot_scene/planets/
    - assets/tarot_scene/planet_rotations/
    - assets/tarot_scene/shooting_stars/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
