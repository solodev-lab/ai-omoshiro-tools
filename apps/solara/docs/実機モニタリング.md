
```
cd E:\AppCreate\apps\solara\worker
```

` して ````
```
npx wrangler tail --format pretty
```

Ctrl+Cで終了

## ログの見方（数分後）

1. アプリで何か操作（Pro 経路を1回踏むと良い）してログを発生させる
2. 2〜3分待つ（ダッシュボードに反映されるまで少しラグあり）
3. Cloudflare ダッシュボード → **Workers & Pages → solara-api → Observability（または Logs）タブ**を開く
4. 検索ボックスに **`would block`** と入れる → 監視期間中に誤ブロックが出ていないか一発で確認できる